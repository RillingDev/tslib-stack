/**
 * Add two numbers
 *
 * @function add
 * @memberof Add
 * @since 1.0.0
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
const add = (a: number, b: number): number => a + b;

export default add;
